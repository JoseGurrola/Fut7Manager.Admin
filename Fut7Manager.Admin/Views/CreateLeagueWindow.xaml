<Window x:Class="Fut7Manager.Admin.Views.CreateLeagueWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Crear liga"
        Width="400"
        Height="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="12"
            Background="White"
            BorderThickness="1"
            BorderBrush="Gray">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Nombre de la liga"
                       Margin="0,0,0,5"/>
            <TextBox Grid.Row="1"
                     Text="{Binding LeagueName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBoxStyle}"
                     VerticalContentAlignment="Center"
                     MaxLength="50"/>
            <!-- imagen -->
            <TextBlock Grid.Row="2" Text="Logo"/>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <!-- URL (se llena automáticamente) -->
                <TextBox Grid.Column="0"
                    Text="{Binding LogoFileName}"
                    Style="{StaticResource ModernTextBoxStyle}"
                    VerticalContentAlignment="Center"
                    IsReadOnly="True"/>
                <!-- Botón subir -->
                <Button Grid.Column="1"  Content="Subir logo"
                   Command="{Binding UploadLogoCommand}" Style="{StaticResource ThirdButtonStyle}"
                   Margin="5,27,5,27" Width="90"
                   />
                <!-- Preview -->
                <Border Width="70" Grid.Column="2" 
                   Height="70"
                   CornerRadius="35"
                   Background="#EEEEEE">
                    <Image Source="{Binding LogoUrl}"
      Stretch="UniformToFill"/>
                </Border>
            </Grid>
            <!-- costo / grupos-->
            <Grid Grid.Row="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="Costo de Inscripción"
                            Margin="0,0,0,5"/>
                <TextBox 
                     Text="{Binding RegistrationFee, Mode=TwoWay}"
                    Style="{StaticResource CurrencyTextBoxStyle}"
                    Grid.Row="1" Grid.Column="0"
                     Height="40"
                     HorizontalAlignment="Stretch"
                     Margin="0,0,10,0"
                     FontSize="16"
                      Padding="4,0,0,0"
                    VerticalContentAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1"
                     Text="Número de grupos"
                        Margin="0,0,0,5"/>
                <ComboBox Grid.Row="1" Grid.Column="1"
          Style="{StaticResource ModernComboBoxStyle}"
          SelectedValue="{Binding NumberOfGroups}"
          SelectedValuePath="Content"
          Height="40" IsEditable="False">
                    
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                    <ComboBoxItem Content="7"/>
                    <ComboBoxItem Content="8"/>
                    <ComboBoxItem Content="9"/>
                    <ComboBoxItem Content="10"/>
                </ComboBox>
            </Grid>
            <StackPanel Grid.Row="5"
                        Height="40"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,15,0,0">
                <Button Content="Cancelar"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="8,0,0,0"
                        Width="90"
                        Command="{Binding CancelCommand}"/>
                <Button Content="Guardar"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="90"
                        Margin="20,0,0,0"
                        IsDefault="True"
                        Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>